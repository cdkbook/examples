// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for ReviewsHandlerFunction
 */
export interface ReviewsHandlerFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/handlers/reviews/reviews-handler.
 */
export class ReviewsHandlerFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: ReviewsHandlerFunctionProps) {
    super(scope, id, {
      description: 'src/handlers/reviews/reviews-handler.lambda.ts',
      ...props,
      runtime: lambda.Runtime.NODEJS_14_X,
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/handlers/reviews/reviews-handler.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}